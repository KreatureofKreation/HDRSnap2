<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HDRSnap2.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HDRSnap2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="14" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Capture hotkey" FontSize="20" FontWeight="SemiBold"/>

        <Button x:Name="RecordButton" Grid.Row="1" HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center" Click="RecordButton_Click" Content="Ctrl+Alt+Q"/>

        <TextBlock x:Name="HintText" Grid.Row="2" Foreground="Gray" TextWrapping="Wrap"
                   Text="Click the button, then press your combo (needs at least one of Ctrl / Alt / Shift / Win)."/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click" Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="Cancel" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
